Installation
============

Run the build script ./build.xml with the following command:

 - ant -Dplay.path=/path/to/play/folder
 
 Copy the whole project (ie the folder .) into /path/to/play/folder/modules/
